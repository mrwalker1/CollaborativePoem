import java.io.*;
public class Player{
    if(game.leftPressed()==true){
        playerPosition -= 5;
    }
    if(game.rightPressed()==true){
        playerPosition += 5;
    }
    player.setXPosition(playerPosition);
    ball.setYPosition(i);
    ball.setXPosition(x);
}